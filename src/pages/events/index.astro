---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BackgroundShader from '../../components/BackgroundShader.astro';
import Navbar from '../../components/Navbar.astro';
import { supabase } from '../../lib/supabase.js';

const { data: events, error } = await supabase
  .from('events')
  .select('id, slug, title, short_description, date, time, venue, mode, poster_url, registration_open, is_featured')
  .order('date', { ascending: true });

if (error) {
  console.error('Failed to load events:', error.message);
}

const hasEvents = Array.isArray(events) && events.length > 0;
---

<BaseLayout title="E-Cell VJIT | Events">
  <BackgroundShader />
  <Navbar />

  <section class="container mx-auto px-6 py-16 md:py-24 relative z-10">
    <div class="max-w-4xl mb-10 md:mb-14">
      <p class="text-xs uppercase tracking-[0.3em] text-red-200/80 mb-4">Events</p>
      <h1 class="text-4xl md:text-6xl font-jakarta font-semibold tracking-tight mb-4">Where the next campus moment will light up</h1>
      <p class="text-gray-300 font-inter text-base md:text-lg leading-relaxed max-w-2xl">
        {hasEvents
          ? 'A living archive of hackathons, workshops, speaker sessions, and campus initiatives.'
          : 'This space is intentionally empty for now, but designed to become a living archive of hackathons, workshops, speaker sessions, and campus initiatives.'}
      </p>
    </div>

    {hasEvents && (
      <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-6 md:gap-7 mb-12 md:mb-16">
        {events.map((event) => (
          <a href={`/events/${event.slug}`} class="glass noise-overlay rounded-2xl p-6 md:p-7 flex flex-col gap-3 hover:bg-white/5 transition-colors">
            <span class="events-ghost-label w-fit">{event.mode}</span>
            <h3 class="text-xl font-jakarta font-semibold tracking-tight text-white">{event.title}</h3>
            <p class="text-gray-300 font-inter text-sm leading-relaxed">{event.short_description}</p>
            <div class="mt-auto pt-3 flex items-center justify-between text-xs text-gray-400 font-inter">
              <span>{new Date(event.date).toLocaleDateString('en-IN', { day: 'numeric', month: 'short', year: 'numeric' })}</span>
              <span>{event.venue}</span>
            </div>
            {event.registration_open && (
              <span class="text-xs uppercase tracking-[0.2em] text-red-200/80">Registration open</span>
            )}
          </a>
        ))}
      </div>
    )}

    <div class="glass noise-overlay events-stage rounded-3xl p-6 md:p-8 lg:p-10 mb-8 md:mb-10" style={hasEvents ? 'display:none' : undefined}>
      <div class="events-stage-inner rounded-2xl p-6 md:p-8 lg:p-10">
        <div class="flex flex-col lg:flex-row lg:items-end lg:justify-between gap-8">
          <div class="max-w-2xl">
            <span class="events-pill inline-flex items-center gap-2 mb-5">
              <span class="h-2 w-2 rounded-full bg-red-400 animate-pulse"></span>
              Empty archive
            </span>
            <h2 class="text-3xl md:text-5xl font-jakarta font-semibold tracking-tight mb-4">A cinematic stage reserved for future E-Cell events</h2>
            <p class="text-gray-300 font-inter text-base md:text-lg leading-relaxed max-w-xl">
              The page stays clean until the first event lands, then it will naturally become the place where students discover what’s happening next.
            </p>
          </div>

          <div class="events-orbit-shell mx-auto lg:mx-0" aria-hidden="true">
            <div class="events-orbit events-orbit-one"></div>
            <div class="events-orbit events-orbit-two"></div>
            <div class="events-orbit events-orbit-three"></div>
            <div class="events-orbit-core">
              <iconify-icon icon="solar:calendar-minimalistic-linear" width="34" height="34"></iconify-icon>
            </div>
          </div>
        </div>

        <div class="mt-10 grid grid-cols-1 md:grid-cols-3 gap-4 md:gap-5">
          <article class="events-ghost-card">
            <span class="events-ghost-label">Hackathons</span>
            <h3>High-energy build spaces</h3>
            <p>Reserved for challenge-driven sprints, prototypes, and fast-moving student teams.</p>
          </article>
          <article class="events-ghost-card">
            <span class="events-ghost-label">Workshops</span>
            <h3>Learning with momentum</h3>
            <p>A future home for practical sessions on startup thinking, problem solving, and execution.</p>
          </article>
          <article class="events-ghost-card">
            <span class="events-ghost-label">Speaker Sessions</span>
            <h3>Founder stories in focus</h3>
            <p>For conversations with builders, mentors, and leaders once the event calendar begins.</p>
          </article>
        </div>
      </div>
    </div>

    <div class="fire-line-divider" aria-hidden="true"></div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 md:gap-7">
      <div class="glass noise-overlay rounded-2xl p-6 md:p-8">
        <p class="text-xs uppercase tracking-[0.3em] text-red-200/80 mb-4">Future Rhythm</p>
        <h2 class="text-2xl md:text-4xl font-jakarta font-semibold tracking-tight mb-4">When events arrive, this section becomes the timeline</h2>
        <p class="text-gray-300 font-inter leading-relaxed">
          For now, it stays as a polished shell with subtle cues, so the page feels complete without pretending there is live content yet.
        </p>
      </div>

      <div class="glass noise-overlay rounded-2xl p-6 md:p-8">
        <p class="text-xs uppercase tracking-[0.3em] text-red-200/80 mb-4">Event Flow</p>
        <div class="space-y-4">
          <div class="events-flow-item"><span>01</span><p>Announcement</p></div>
          <div class="events-flow-item"><span>02</span><p>Registration</p></div>
          <div class="events-flow-item"><span>03</span><p>Execution</p></div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .events-stage {
    border: 1px solid rgba(248, 113, 113, 0.18);
    background: linear-gradient(165deg, rgba(24, 5, 4, 0.92), rgba(10, 2, 2, 0.92));
  }

  .events-stage-inner {
    border: 1px solid rgba(255, 255, 255, 0.08);
    background: radial-gradient(120% 120% at 50% 0%, rgba(185, 28, 28, 0.2) 0%, rgba(9, 9, 11, 0.25) 45%, rgba(6, 2, 2, 0.52) 100%);
  }

  .events-pill {
    border: 1px solid rgba(248, 113, 113, 0.28);
    border-radius: 999px;
    padding: 0.35rem 0.7rem;
    color: rgb(254 202 202);
    font-size: 0.72rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    background: rgba(127, 29, 29, 0.16);
  }

  .events-orbit-shell {
    position: relative;
    width: min(100%, 310px);
    aspect-ratio: 1 / 1;
    border-radius: 999px;
  }

  .events-orbit,
  .events-orbit-core {
    position: absolute;
    inset: 0;
    border-radius: 999px;
  }

  .events-orbit {
    border: 1px solid rgba(248, 113, 113, 0.14);
  }

  .events-orbit-one {
    inset: 0.5rem;
    animation: orbitPulse 10s ease-in-out infinite;
  }

  .events-orbit-two {
    inset: 1.6rem;
    border-color: rgba(251, 146, 60, 0.18);
    animation: orbitPulse 12s ease-in-out infinite reverse;
  }

  .events-orbit-three {
    inset: 3.2rem;
    border-color: rgba(254, 202, 202, 0.12);
  }

  .events-orbit-core {
    inset: 4.4rem;
    display: grid;
    place-items: center;
    color: rgb(254 202 202);
    background: radial-gradient(circle, rgba(185, 28, 28, 0.26), rgba(9, 9, 11, 0.2));
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.05);
  }

  .events-ghost-card {
    min-height: 180px;
    border-radius: 1rem;
    border: 1px dashed rgba(248, 113, 113, 0.18);
    background: linear-gradient(165deg, rgba(127, 29, 29, 0.16), rgba(6, 2, 2, 0.36));
    padding: 1.15rem;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    gap: 0.45rem;
  }

  .events-ghost-card h3 {
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 1.15rem;
    font-weight: 600;
    color: rgb(255 247 247);
  }

  .events-ghost-card p {
    color: rgb(209 213 219);
    line-height: 1.65;
    font-size: 0.95rem;
  }

  .events-ghost-label {
    width: fit-content;
    border-radius: 999px;
    padding: 0.28rem 0.58rem;
    font-size: 0.68rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgb(254 202 202);
    border: 1px solid rgba(248, 113, 113, 0.22);
    background: rgba(127, 29, 29, 0.14);
  }

  .events-flow-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.9rem 1rem;
    border-radius: 0.9rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    background: rgba(255, 255, 255, 0.02);
  }

  .events-flow-item span {
    width: 2.1rem;
    height: 2.1rem;
    display: inline-grid;
    place-items: center;
    border-radius: 999px;
    border: 1px solid rgba(248, 113, 113, 0.22);
    background: rgba(127, 29, 29, 0.18);
    color: rgb(254 202 202);
    font-size: 0.78rem;
    letter-spacing: 0.08em;
  }

  .events-flow-item p {
    color: rgb(244 244 245);
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-weight: 500;
  }

  @keyframes orbitPulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.9;
    }
    50% {
      transform: scale(1.02);
      opacity: 1;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .events-orbit-one,
    .events-orbit-two {
      animation: none;
    }
  }
</style>