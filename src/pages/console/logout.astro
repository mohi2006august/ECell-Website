---
export const prerender = false;

import { SESSION_COOKIE_NAME } from '../../lib/adminAuth.js';

Astro.cookies.delete(SESSION_COOKIE_NAME, { path: '/console' });
return Astro.redirect('/console/login');
---
